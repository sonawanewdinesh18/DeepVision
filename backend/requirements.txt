# DeepVision Backend Dependencies
# FastAPI & Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Data Validation & Configuration
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0
python-dotenv>=1.0.1

# Database & Storage
supabase>=2.4.0

# Authentication & Security
python-jose[cryptography]>=3.3.0

# HTTP Client
httpx>=0.27.0

# Media Processing
Pillow>=10.3.0
opencv-python-headless>=4.8.0,<4.11.0
numpy>=1.26.0,<2.0.0

# Typing & Utilities
typing-extensions>=4.11.0

# NOTE: torch and torchvision are installed separately in the Dockerfile
# using CPU-only wheels from download.pytorch.org/whl/cpu
# DO NOT add them here — pip will pull the 800MB CUDA build instead.
